opencv-python>=4.8
numpy>=1.24
# Захват окна (ускоренный скриншот эмулятора на Windows вместо ADB).
# Обязательно только если CAPTURE_METHOD=window в tools/cfg/config.json.
mss>=9.0
pywin32>=306 ; sys_platform == "win32"
